<launch>
  <arg name="config" default="$(find pod_detector)/config/detector.yaml" />
  <node pkg="pod_detector" type="pod_detector_node" name="pod_detector" output="screen">
    <rosparam command="load" file="$(arg config)" />
  </node>
</launch>
